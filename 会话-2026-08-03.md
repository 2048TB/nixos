# 会话上下文 · 2026-08-03

## 任务
分析 `nixos-config-main/`（用户另一台主机上的 NixOS 配置），指出低效/混乱之处，优化完善、提高代码质量，使其更科学、易用，**特别面向 NixOS 新手**。

## 仓库概况（首轮勘察）
- Flake 仓库：NixOS 25.11 + nix-darwin + home-manager，多主机（x86_64-linux + aarch64-darwin）
- 入口：`flake.nix` → `nix/hosts/outputs/`（outputs 全部委托进去）
- 关键目录：
  - `nix/lib/`：自建工具库（host-registry、mkNixosHost/mkDarwinHost、attrs、validation、display-topology 等 15 个文件）
  - `nix/modules/core/`：跨平台核心模块（options.nix 10KB 自定义 options + roles/ + _mixins/）
  - `nix/home/`：home-manager（base/linux/darwin + configs/ 纯配置文件）
  - `nix/hosts/`：outputs/ + registry/systems.toml（TOML 主机注册表 + JSON schema）
  - `nix/scripts/admin/`：9 个 bash 脚本（安装/sops/守卫/格式检查）
  - `secrets/`：sops-nix 加密
  - `justfile`：update/switch/upgrade/clean/self-check/validate-local
- 文档：README + docs/README(19KB) + ENV-USAGE + NIX-COMMANDS + KEYBINDINGS + 各目录 README

## 已读文件
- flake.nix、README.md、justfile
- nix/lib/default.nix（281 行：nixosSystem/macosSystem 包装、unfree 白名单、TOML registry 读取、CPU/GPU 推导）
- nix/lib/host-registry.nix（186 行：registry 校验断言，很长的 assertMsg 链）
- nix/lib/mkNixosHost.nix（163 行：主机组装 + 路径断言）

## 初步观察（待深入）
1. 架构是"过度工程"倾向：registry TOML + JSON schema + vars.nix 双源 + 断言库 + capabilities 推导——对新手极不友好
2. flake.nix 中 cherry-studio 钉死 commit 的 TODO
3. lib/default.nix 混杂：系统组装 + unfree 策略 + 包解析 + KVM 推导全在一个文件
4. 长 assertMsg && 链（host-registry.nix 84-184）可读性差
5. justfile 依赖 print-flake-repo.sh 间接层

## 待办
- [x] 读 hosts/outputs/*、registry/systems.toml、hosts README
- [x] 读 modules/core/*（options.nix、default.nix、assertions.nix 等）
- [x] 读 home/*（base、linux、darwin）
- [x] 读 docs/README.md 了解设计意图
- [x] 汇总问题清单 → 分级（结构性/代码质量/新手友好性）
- [x] 制定优化方案（发决策卡确认方向）→ 用户选择：直接重构（方案A）+ 全部三项
- [ ] 实施重构

## 重构决定（用户已拍板 2026-08-03 16:50）
- 方案A：直接动手重构，保持行为不变，删除过度工程层
- 范围：合并 registry+vars.nix 单一 hosts.nix、精简 lib、新手文档 全部要

## 实施方案（新会话接手后细化，2026-08-03 17:00 起实施）

### 目标架构
1. **`nix/hosts/default.nix`（新建）**：全仓库唯一主机清单，合并
   `registry/systems.toml` + `registry/systems.schema.json` + 各主机 `vars.nix` + `_shared/vars-common.nix`。
   共享默认值放 `nixosDefaults`，每台主机 `nixosDefaults // {…}` 覆盖。
   gpuMode 全部显式声明（原 zl/zzly 靠 hardware-modules 推导 amdgpu，行为等价）。
2. **字段校验下沉到模块层**（本来就存在，属重复防线）：
   options.nix types.enum + assertions.nix + _shared/checks/host-metadata.nix 保留；
   删除 lib/host-registry.nix 的 100 行 assertMsg 链、TOML schema、schema-sync 脚本。
3. **lib 精简**：15 文件 → 11 文件
   - 删 host-registry.nix；host-meta.nix + host-capabilities.nix 合并为 host.nix（去掉死键
     optionalStringOptions/optionalNullableStringOptions/registryOwnedKeys/requiredRegistryKeys/
     allowedLinux(Darwin)DesktopProfiles）
   - nixosSystem/macosSystem 内联进 mkNixosHost/mkDarwinHost（唯一调用点）
   - 删 gpuModeFromHardwareModules（gpuMode 已显式）、attrs.nix 的 namesNotMatching/discoverHostNamesBy
   - default.nix 去掉 TOML 读取，改 `hosts = import ../hosts`
4. **outputs 精简**：common.nix 删 mkRegistryState/assertRegistryState（双重记账消失，
   主机来源=清单 filterAttrs by system）；eval-tests.nix 只留 headless 防 GUI 泄漏检查
   （删测试断言机器本身的 registryRequiredKeyCheck、missingHomeUserCheck）。
5. **脚本/CI 同步**：删 host-meta-schema-sync.sh；justfile 删 _registry-schema-check/
   _registry-meta-sync-check；sops.sh primary_user_name 改读 nix/hosts/default.nix；
   bootstrap-install.sh 主机存在性检查改 hardware.nix；workflow 不再装 check-jsonschema；
   devShell 移除 check-jsonschema。
6. **文档**：新增 docs/GETTING-STARTED.md 新手指南；重写 README/hosts README/outputs README；
   更新 docs/README.md、NIX-COMMANDS.md；appearance.kdl 注释。

### 行为差异声明（唯一允许的偏差）
- 元数据校验时机从「flake eval 即断言」变为「模块求值时断言」（nix flake check / rebuild 仍拦截）
- 检查面减少：registry 自测类 checks 删除；主机配置本身的 eval checks 全保留
- 错误路径差异：目录存在但未登记 → 原来 eval 报错，现在静默忽略（清单为准）

### 已删文件清单
- nix/hosts/registry/{systems.toml,systems.schema.json}
- nix/hosts/nixos/{zky,zly,zl,zzly}/vars.nix、nix/hosts/nixos/_shared/vars-common.nix
- nix/hosts/darwin/zly-mac/vars.nix
- nix/lib/{host-registry.nix,host-meta.nix,host-capabilities.nix}（后两者并入 host.nix）
- nix/scripts/admin/host-meta-schema-sync.sh

### 验证方式
- 本机 Windows 无 nix；用 docker nixos/nix 镜像跑 `nix flake check --all-systems --no-build`
- 对照重构前后 nixosConfigurations 属性名、mainUser、检查清单

## 实施进度（17:05 新会话接手实施,已通读全部相关文件）
- [x] nix/hosts/default.nix（清单建成:nixosDefaults + 4 NixOS 主机 + zly-mac,gpuMode 全显式）
- [x] lib 重构（删 host-registry;host-meta+host-capabilities→host.nix;nixosSystem/macosSystem 内联进 mkNixosHost/mkDarwinHost;default.nix 去 TOML 改 hosts=import ../hosts;attrs.nix 删 namesNotMatching/discoverHostNamesBy）
- [x] outputs 重构（common.nix 删 mkRegistryState/assertRegistryState;两平台入口 filterAttrs by system;eval-tests 只留 headless 检查,签名改 {lib,mylib,args}）
- [x] 脚本/justfile/workflow（justfile 删两个 registry recipe;workflow 去 pipx/check-jsonschema;sops.sh 读清单;bootstrap-install 检查 hardware.nix;devShell 去 check-jsonschema;storage.nix/steam.nix 文案）
- [x] 文档（GETTING-STARTED 新建;hosts/nixos/outputs 三个 README 重写;docs/README、NIX-COMMANDS、ENV-USAGE、根 README、AGENTS 更新）
- [x] docker 验证全部通过（nixos/nix 容器）:
  1. 全部 .nix 解析 OK
  2. 清单 eval OK;nixosConfigurations=["zky","zl","zly","zzly"],darwin=["zly-mac"],homeConfigurations 5 个,与重构前一致
  3. 关键字段对照一致:zky nvidia/32G/2990172/[vpn];zl amdgpu/128G/null/4roles;zly hybrid/32G/7182282/4roles;zzly amdgpu/32G/1513128/[vpn]
  4. nix flake check --all-systems --no-build → all checks passed!
  5. checks.x86_64-linux.pre-commit-check 实际构建通过（nixpkgs-fmt/statix/deadnix 全绿;修了 2 处折行 + 存量 ssh-access.nix 空模式 W10）
  6. checks.x86_64-linux.format-sanity 实际构建通过
  7. shellcheck 0.11 + shfmt 对全部 admin 脚本与 pre-commit hook 通过

## 结论
重构完成且验证等价。待用户在真机上 `just validate-local` + `just host=<主机> switch` 做最终确认。
唯一顺手修的存量问题:zly/ssh-access.nix `{ ... }:` → `_:`（statix W10,全量 check build 才暴露的 lint 债）。

---

# 第二阶段:继续优化其它模块（17:21 用户指示）

## 任务
主机架构层已完成,继续优化剩余模块:nix/modules/core/、nix/home/、overlays/pkgs、flake.nix(cherry-studio pin TODO)等。

## 勘察进度
- [x] 通读 nix/modules/core/*.nix + roles/ 全部 7 个 role
- [x] 通读 nix/home/(base/linux/darwin) 全部文件
- [x] overlays/pkgs/lib 余部(theme/display-topology/launchers/nix-cache/portal-config)/ml-shell/主机扩展文件
- [x] 实施与验证

## 勘察结论
第二阶段勘察发现代码质量整体很高(注释充分、结构一致、有 eval-check 护栏),不制造无谓改动。

## 已实施(全部经 docker 验证)
1. **docker.nix 一致性**:改用 roleFlags.useRootlessDocker/useRootfulDocker,不再自算 hostCfg.dockerMode
   → 验证:zl/zly rootless=true+linger=true,zky/zzly=false/null,与改前一致
2. **home/darwin 幽灵键**:enableHmReleaseCheck(无主机使用、无文档)删除,与 Linux 侧一致硬编码 true;顺带清掉不再用的 hostCfg/resolve-host 引用
3. **cherry-studio pin 之谜查明并归档**(重要):
   - 容器实测三来源版本:pin=1.7.9,stable25.11=1.7.9,unstable=1.9.3
   - 尝试移除 pin 改用 stable → flake check 失败:stable 的 cherry-studio 1.7.9 依赖被标 insecure 的 electron-38.8.4,eval 拒绝
   - **这正是当年未记录的 pin 原因**。已回滚,并把查明的原因写进 flake.nix 注释(原 TODO 变为有据可查)
   - 替代路径:升级 unstable 1.9.3(electron 在维护)→ 需用户真机验证,已发决策卡
4. flake.lock 由 nix flake lock 重新生成(同 commit,验证通过)

## 有意不动的项(避免churn)
- mkSedDeleteExpr 在 files.nix/launchers.nix 的 5 行相似(两个 wrapper 语义不同,合并增加耦合)
- roleFlags 惯用法(每模块 2 行)不改成 options(会牵动 checks)
- package-groups 的 check-jsonschema(用户 CLI 工具)
- ml-shell/ml-stack 的 CUDA 环境变量结构(用途明确)

## 第二阶段验证记录(docker nixos/nix)
- cherry-studio 回 pin 后 = 1.7.9 ✓;docker 模式逐机对照一致 ✓
- pre-commit(nixpkgs-fmt/statix/deadnix)+ format-sanity 实际构建通过 ✓
- nix flake check --all-systems --no-build → all checks passed! ✓
- 主机名单与 homeConfigurations 不变 ✓

## 用户决策(17:35)
- cherry-studio:**直接不装**(决策卡选 drop)

## cherry-studio 移除完成(17:40,已验证)
- flake.nix 删除 nixpkgs-cherry-studio 输入;outputs/default.nix 删 pkgsCherryStudio;packages.nix 删参数与包引用
- flake.lock 由 nix flake lock 裁剪("Removed input 'nixpkgs-cherry-studio'")
- 验证:四台主机 home.packages 中 cherry* 匹配全为空 ✓;lint 构建通过 ✓;flake check all checks passed ✓;主机/homeConfigurations 名单不变 ✓
- 收益:少一个完整 nixpkgs 输入(update 时少下载一份 nixpkgs),卸掉带 EOL electron 的应用

## 第二阶段结束状态
全部工作完成并验证。剩余待用户真机操作:just validate-local + just host=<主机> switch。

---

# 第三阶段:继续优化(17:40 用户指示)

## 勘察范围(全部完成)
- [x] nix/scripts/admin 全部 8 个脚本(common/install-live/update-flake/print-flake-repo/guard-secrets 本轮补读)
- [x] nix/home/base/config-files.nix
- [x] 剩余主机文件:disko.nix(一行引共享)、disko-luks-btrfs.nix、各 hardware.nix、hardware-gpu-hybrid.nix、zly-mac home.nix/checks.nix
- [x] _shared/checks 组抽查(boot.nix 等,由 context.nix 驱动,结构好)
- [x] CLAUDE.md

## 勘察结论(如实)
脚本层质量非常高:安全删除护栏(assert_safe_child_path)、原子写(sops encrypt 先临时文件再 mv)、破坏操作 token 确认、key 与 pub 匹配校验、Git tracked-file allowlist 同步。主机层全是薄包装。**没有值得动的"低效混乱"**。

## 第三阶段已实施(验证全绿)
1. outputs 死导出清理:x86_64-linux/aarch64-darwin 的 `data`/`registeredHosts`(无任何消费者)+ common.nix collectHostData 返回值同步裁剪
2. CLAUDE.md 关联入口补 GETTING-STARTED(与 AGENTS.md 对齐)
3. 验证:lint 构建通过 + flake check all checks passed + 主机名单不变

## 三阶段总账
- 阶段1:主机架构重构(registry+schema+vars 三层 → 单一清单 nix/hosts/default.nix),lib 13→11 文件,新手文档
- 阶段2:docker.nix 一致性、darwin 幽灵键、cherry-studio pin 查明原因后按用户决策彻底移除(连带删一个 nixpkgs 输入)
- 阶段3:脚本/主机/checks 全面勘察确认健康,清理 outputs 死导出,文档入口对齐
- 全程 docker nixos/nix 验证:每轮改动都过 lint 实际构建 + flake check --all-systems

## 最终状态
代码侧工作已全部完成。待用户真机:同步仓库 → just validate-local → just host=<主机> switch。

---

# 第四阶段:清理更新全部文档 + 同步 GitHub(17:46 用户指示)

## 计划
1. 全文档过时引用扫描(registry/vars.nix/systems.toml/check-jsonschema/cherry-studio/host-meta 等关键词)
2. 逐文档核对与修正(README/docs/*/hosts README/home README/secrets README/AGENTS/CLAUDE/KEYBINDINGS)
3. 本目录无 .git(zip 快照);检查本机 git/gh 认证 → git init + 接 origin/main 历史 → 按主题分多个 Conventional Commit → guard-secrets 通过后 push
4. 远程仓库:README 中的 https://github.com/2048TB/nixos-config.git

## 进度
- [ ] 文档扫描与修正
- [ ] git 环境检查与仓库初始化
- [ ] 分主题提交
- [ ] guard-secrets + push

已删文件:registry/{systems.toml,schema.json}、5 个 vars.nix、vars-common.nix、host-meta-schema-sync.sh、lib/{host-registry,host-meta,host-capabilities}.nix
残留检查:rg 确认无 vars.nix/systems.toml/hostRegistry 等悬空引用（仅留 3 处有意保留:roleFlags、用户包 check-jsonschema、checks/host-metadata.nix）

### 实施补充笔记
- package-groups.nix 里的 check-jsonschema 是用户 CLI 工具,按计划仅移出 devShell,不动用户包
- 模块层引用需同步:storage.nix 报错文案(vars.nix→清单)、steam.nix 注释、ENV-USAGE.md
- eval-tests.nix 新签名 { lib, mylib, args }(去掉 common)
- mkNixosHost 保留结构性断言(hardware/disko 路径 + 身份字段),指向清单标签
- 所有新 nix 文件须过 nixpkgs-fmt/statix/deadnix(pre-commit-check 是 flake check 的一部分)
- appearance.kdl 已有完整中文注释,该计划项视为已满足,跳过
